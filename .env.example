
#Google Spreadsheet ID (obtenido de la URL de tu hoja de cálculo)
SPREADSHEET_ID="your_spreadsheet_id_here"

#token del bot de Telegram (obtenido de BotFather)
BOT_TOKEN="your_bot_token_here"

# Path to the Google Service Account credentials JSON (where to place the downloaded file)
# 1. In Google Cloud Console -> IAM & Admin -> Service Accounts
# 2. Create a service account, grant 'Editor' or the minimal Sheets scopes
# 3. Create a key (JSON) and download it
# 4. Place the downloaded file at: config/credentials.json
CREDENTIALS_PATH="config/credentials.json"

